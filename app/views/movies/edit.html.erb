<h1>Edit movie</h1>

<% @movie.errors.full_messages.each do |message| %>
  <p style="color: red;"><%= message %></p>
<% end %>

<%= form_with(url: movie_path(@movie), method: :patch, data: { turbo: false }) do %>
<!--  <input name="authenticity_token" value="<%#= form_authenticity_token %>" type="hidden">
  
  <input name="_method" value="patch" type="hidden">-->

  <div>
    <%= label_tag :title %>
<!--    <label for="title">
      Title
    </label>-->

    <%= text_field_tag "movie[title]", @movie.title, { id: "title" } %>
<!--    <input type="text" id="title" name="title" value="<%#= @movie.title %>">-->
  </div>

  <div>
    <%= label_tag :description %>
<!--    <label for="description">
      Description
    </label>-->

    <%= text_area_tag "movie[description]", @movie.description, { id: "description" } %>
<!--    <textarea id="description" name="description" rows="3"><%#= @movie.description %></textarea>-->
  </div>

  <%= button_tag "Update Movie" %>
<!--  <button>
    Update Movie
  </button>-->
<% end %>


<!--<form action="<%#= movie_path(@movie) %>" method="post" data-turbo="false">
  <input name="authenticity_token" value="<%#= form_authenticity_token %>" type="hidden">
  
  <input name="_method" value="patch" type="hidden">

  <div>
    <label for="title">
      Title
    </label>

    <input type="text" id="title" name="title" value="<%#= @movie.title %>">
  </div>

  <div>
    <label for="description">
      Description
    </label>

    <textarea id="description" name="description" rows="3"><%#= @movie.description %></textarea>
  </div>

  <button>
    Update Movie
  </button>
</form>-->
